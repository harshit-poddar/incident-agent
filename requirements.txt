pydantic>=2.6
pydantic-settings>=2.2
fastapi>=0.110
uvicorn>=0.29
openai>=1.30
pytest>=8.0
httpx>=0.27
psycopg[binary]>=3.1
qdrant-client>=1.7
redis>=5.0

# Optional -- only needed for TRACE_MODE=otel (the default, memory, needs none).
#   pip install opentelemetry-sdk opentelemetry-exporter-otlp
# opentelemetry-sdk>=1.24
# opentelemetry-exporter-otlp>=1.24
