[project]
name = "incident-agent"
version = "0.1.0"
description = "AGENTS_026 - Autonomous incident diagnosis & resolution agent"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py310"
